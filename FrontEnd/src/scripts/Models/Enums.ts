export enum LobbiesEnum {
    IN_LOBBY = 1,
    WAITING = 2,
    DENIED = 3,
    MENU = 4
}

export enum TileTypeEnum {
    GROUND = 373,
    WALL = 92,
    PLAYER = 100,
}

export enum SizeEnum {
    TILE_SIZE = 16,
    PLAYER_NAME_OFFSET = 15
}
    